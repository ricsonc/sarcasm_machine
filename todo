get labeled data 
get unlabeled data
implement the (deep bidirectional ltsm) neural network


ideas:
using precomputed unsupervised word embedding
use part of speech as a feature
character level sarcasm detection
maybe correlate misspelly-ness with sarcasm?
